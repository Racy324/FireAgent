# Optional API base URL for deployments where the frontend and backend are not served from
# the same origin. Leave empty to use the current origin, Vite dev proxy, or Nginx proxy.
VITE_API_BASE_URL=
